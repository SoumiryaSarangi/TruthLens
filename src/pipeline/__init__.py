"""The pipeline: contracts, registry, orchestrator, batch runner.

SYSTEM_DESIGN.md §1: the served pipeline and the evaluated pipeline are the
same code. Nothing here imports a model library at module scope.
"""
